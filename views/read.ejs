<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="min-w-screen min-h-screen bg-zinc-900 p-3 text-white">

            <div class="nav mb-10">             
               <a href="/" class="text-blue-300">back to home</a>
</div>
        <h1 class="text-3xl tracking-tighter mb-3">All users</h1>
        
        <!-- flex row for cards -->
        <div class="users flex gap-5 flex-wrap">
            <% if(users.length>0){ %>
                


                 <% users.forEach(function(user){ %>
            <div class="user w-72 flex-shrink-0 p-4 bg-zinc-800 rounded-lg">
                <div class="w-full h-52 rounded-lg bg-red-500 overflow:hidden">
                    <img class="w-full h-full object-cover object-top " src="<%= user.image %>" alt="">
                </div>
                <h3 class="text-xl tracking-tighter mt-3"><%= user.name %></h3>
                <h5 class="text-zinc-500"><%= user.email %></h5>
                <div class="flex gap-5">
                    <a class="text-zinc-300" href="/edit/<%= user._id %>">edit</a>
                    <a class="text-red-500" href="/delete/<%= user._id %>">delete</a>
                </div>
            </div>
            <% }) %>
                <% }else{ %>
                    <p class="text-zinc-500">No users found</p>
                <% } %>
        </div>
    </div>
</body>
</html>
